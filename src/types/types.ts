// DS: Data Structure
export type DataStructureSearchInfo = {
  name: string;
  link: string;
};
export type DataStructureDetails = {
  code: string;
  complexity: { [key: string]: string };
  description: string;
};

// A: Algorithm
